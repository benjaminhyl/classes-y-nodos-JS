/* function calcSum(numbers) {
    return numbers.reduce((sum, item) => (
       sum = sum + item),0)
  } */
let test = [1,2,3,4];
let res = test.reduce((sum, item) => (
  sum = sum + item),0)
  console.log(res);  